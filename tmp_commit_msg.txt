feat: 修复趣味投注列表请求旧状态与竞态加载的问题

1. 取消闭包陷阱（Stale Closure）：
   - 将原先在 useEffect 内部作为闭包调用的 getSearchKeyword 函数改为直接在渲染作用域内计算为 keyword 变量。
   - 修正了 API 因依赖状态更新不及时而错误传入默认词 "World Cup"，进而导致只有 5 场数据的问题。

2. 导入 AbortController 消除竞态冲突：
   - 引入浏览器原生的信号控制 AbortController。
   - 在用户切换页面分类（触发重新渲染与请求）时，如果在上一个请求还没完成时立刻执行了新请求，会自动调用 controller.abort() 关闭过期的垃圾请求。
   - 保障前端列表严格显示并呈现最后一次有效选择的高亮状态（成功稳定在 17 场）。
