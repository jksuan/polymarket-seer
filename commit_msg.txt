feat(ui): 锁死 Challenge 挑战页面全屏布局并重构 TopHeader 吸顶能力

1. 根治沉浸式交互回弹：重构了 ChallengePage 的核心布局容器，采用 `fixed inset-0` 脱离原生文档流，辅以溢出隐藏。彻底在物理层面上锁死了挑战页面的全局滚动，消除了左右滑卡时引发浏览器原生橡皮筋回弹及地址栏伸缩的干扰，实现深层次类原生沉浸体验。
2. 架构下沉：对齐了全局导航行为，将 `sticky top-0` 与带毛玻璃滤镜底色的 CSS 规则直接写入 TopHeader.tsx 主容器，赋予其原生跨越层叠吸顶的能力。
