---
description: 源码 UTF-8 与 Windows 下避免乱码的写入约定
alwaysApply: true
---

# 源码编码与写入方式

## 必须遵守

- 读写仓库内文本文件（尤其 `.ts` / `.tsx` / `.json`）时，**一律显式 UTF-8**，禁止依赖系统默认编码。
- **禁止**用 PowerShell 管道把「大段含中文或非 ASCII」内容重定向写入源码文件（易导致字面量 `?` 或非法 UTF-8）。
- 批量改 UI 文案：优先 **IDE / 补丁 / Python 或 Node 且 `utf-8` 读写**；避免 Shell 当转码层。

## 建议

- 含中文或 `•`、`≈` 等符号时，若环境编码不可靠，可暂用 `\uXXXX` 写入再在 UTF-8 安全环境下还原为直写中文。
- 提交前对相关文件跑 `eslint` 与 `tsc --noEmit`（或项目既有脚本）。

## 说明

「乱码」多为 **写入链路编码错误**，不是浏览器语言导致。遵守以上约定可降低复发概率。
